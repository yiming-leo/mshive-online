name: Release On Aliyun

on:
  push:
    branches:
      - release_aliyun

jobs:
  build:
    name: Build with NPM
    runs-on: ubuntu-latest
    steps:
      #---------check out-------
      - name: Checkout Repository
        uses: actions/checkout@v3
      #---------set up npm-------
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18.19.0'
      #---------npm install-------
      - name: Install Dependencies
        run: npm install
      #---------npm build-------
      - name: Build Project
        run: npm run build
      #---------upload artifact to other job-------
      - name: Archive artifacts
        uses: actions/upload-artifact@v2
        with:
          name: dist
          path: dist

  deploy:
    name: Deploy to Server
    needs: build
    runs-on: ubuntu-latest
    steps:
      #---------receive artifact to current job-------
      - name: Download artifacts
        uses: actions/download-artifact@v3
        with:
          name: dist
          path: .

      - name: Locate Current Directory & User
        run: |
          id
          set -x
          whoami
          ls
          pwd
          find -name "dist"
          ls
          pwd
#          ll -a

#      - name: SCP Jar to Server
#        uses: appleboy/scp-action@master
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: lym
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          port: ${{ secrets.SERVER_PORT }}
#          source: target/mshive-backend-0.0.1-SNAPSHOT.jar
#          target: /mydata/project_file/mshive-backend-0.0.1-SNAPSHOT.jar

#  run:
#    name: Run Jar on Server
#    needs: deploy
#    runs-on: ubuntu-latest
#    steps:
#      - name: SSH into Server and Run Jar
#        uses: appleboy/ssh-action@master
#        with:
#          host: 47.102.196.219
#          username: lym
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          port: ${{ secrets.SERVER_PORT }}
#          script: |
#            cd /mydata/project_file/
#            java -jar mshive-backend-0.0.1-SNAPSHOT.jar
